server:
  port: 8083
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/KellyCardenas/paymentchain
          search-paths: config-data
  application:
    name: config-service
